# AI Spend CFO environment variables.
# Copy to .env.local for local development. Never commit real values.

# Anthropic API key for the live upload-memo route (app/api/memo/route.ts) and
# the Haiku value-tag classification call. Read by server code only; the value
# is set by the human on Vercel (human gate, before Batch B4). The sample-data
# hero memo is precomputed and committed, so it needs no key at runtime.
ANTHROPIC_API_KEY=
